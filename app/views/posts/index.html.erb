<div class="contents" >
  <p>最新の投稿一覧</p>
  <div class="contents_list">
    <% @posts.each do |post| %>
      <%= render partial: "post", locals: { post: post } %>
    <% end %>
  </div>
  <%= paginate(@posts) %>
</div>